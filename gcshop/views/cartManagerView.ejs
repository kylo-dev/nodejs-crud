<!-- 컴퓨터공학과 201935247 김현겸 -->
<h2 class="fw-bold mb-3">관리자 장바구니 관리 페이지</h2>
<table class="table table-bordered border-primary text-center" style="width: 1300px;">
    <% if (haveCart) { %>
        <thead>
            <td class="fw-bold">사용자</td>
            <td class="fw-bold">상품</td>
            <td class="fw-bold">상품명</td>
            <td class="fw-bold">단가</td>
            <td class="fw-bold" style="width: 150px;">상품 관리</td>
        </thead>
        <% var i = 0
             while (i<list.length) { %>
                <tr>
                    <td class="align-middle"><%=list[i].loginid%></td>
                    <td class="align-middle"><img src="<%=list[i].image%>" alt="merchandise image" style="width: 20%;"></td>
                    <td class="align-middle"><%=list[i].name%></td>
                    <td class="align-middle"><%=list[i].price%></td>
                    <td class="align-middle">
                        <a href="/purchase/cart/update/<%=list[i].cart_id%>" class="d-block m-2 btn btn-outline-primary">수정</a>
                        <a href="/purchase/cart/delete/<%=list[i].cart_id %>" class="d-block m-2 btn btn-outline-danger" onclick="if(confirm('정말로 삭제하시겠습니까?')==false){return false; }">삭제</a>
                    </td>
                </tr>
        <% i += 1 } %>
</table>

<nav aria-label="Page navigation example" class="mb-4">
    <ul class="pagination justify-content-center">
        <% for (var j = 1; j <= totalPages; j++) { %>
            <li class="page-item <%= j == pageNum ? 'disabled' : '' %>">
                <a class="page-link" href="#"><%= j %></a>
            </li>
        <% } %>
    </ul>
</nav>

<% }
    else { %>
    <tr>
        <td>자료 없음</td>
    </tr>
</table>
<% } %>